DEFINE OBJECT o AS (200,300);
DEFINE FLOAT f AS ANGLE BETWEEN o, (200,100);
APPLY [0,1] TO o FOR 5;
DEFINE INT counter AS 0;
LOOP (counter < 100) APPLY [f,1] TO o FOR 1;
SET f AS ANGLE BETWEEN o, (100,100); SET counter AS counter + 1; ENDLOOP;