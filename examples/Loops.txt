DEFINE OBJECT o AS (400,500);
DEFINE FLOAT f AS ANGLE BETWEEN o, (400,400);
APPLY [0,1] TO o FOR 5;
DEFINE INT counter AS 0;
LOOP (counter < 100) APPLY [f,1] TO o FOR 2;
SET f AS ANGLE BETWEEN o, (400,400); SET counter AS counter + 1; ENDLOOP;