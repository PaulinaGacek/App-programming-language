from utils.Action import Action

class Frame:

    def __init__(self, previous: Frame, next:Frame, actions: List[Action])
        self.previous = previous
        self.next = next
        self.actions = Action 
        self.is_updated = False
        self.localisations = {} # mapps object to its localisation
        self.velocity = {} # mapps object to its velocity  
    
    def copy_layout_from_previos_frame():
        if previos is None:
            print("Previos frame is None")
            return
        
        
    
